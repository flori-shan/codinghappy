dockerfile
FROM ubuntu
RUN apt-get update && apt-get install -y openssh-server openssh-client curl vim nginx && apt-get autoclean && apt-get clean && apt-get autoremove && mkdir /var/run/sshd && echo "root:12
3" | chpasswd && echo "PermitRootLogin yes" >> /etc/ssh/sshd_config
EXPOSE 80
EXPOSE 22
CMD ["/usr/sbin/sshd", "-D"]